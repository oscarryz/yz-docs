counter: {
    count: 0
    increment #(amount Int) {
        count = count + amount
    }
    value: { count }
}

main: {
    counter.increment(1)
    n Int = counter.value()
    counter.increment(n)
    m Int = counter.value()
    print("${m}")
}
