To ask: _How would Yz do X_ and explore answers. 

Files on the root directory are open questions and marked with `open-question`
Solved questions are moved to the `solved/` directory and marked with `#answered` or `#rejected` ideally with an explaination and if applicable moved to the `Features/` with enough explaination
